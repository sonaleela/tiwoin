import { SwiperOptionDirective } from './swiper-option.directive';

describe('SwiperOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperOptionDirective();
    expect(directive).toBeTruthy();
  });
});
