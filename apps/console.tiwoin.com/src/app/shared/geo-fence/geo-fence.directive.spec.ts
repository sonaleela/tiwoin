import { GeoFenceDirective } from './geo-fence.directive';

describe('GeoFenceDirective', () => {
  it('should create an instance', () => {
    const directive = new GeoFenceDirective();
    expect(directive).toBeTruthy();
  });
});
