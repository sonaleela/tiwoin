import { GeoLocationDirective } from './geo-location.directive';

describe('GeoLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new GeoLocationDirective();
    expect(directive).toBeTruthy();
  });
});
